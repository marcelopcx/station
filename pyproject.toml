[project]
name = "airtek-game-station"
version = "1.0.0"
description = "Runtime genérico de Game Station: FSM, WebRTC, display, audio e input. El juego vive en otra imagen."
requires-python = ">=3.12"
readme = "README.md"

[tool.setuptools.packages.find]
include = ["controller*"]
